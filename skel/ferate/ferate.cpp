#include <iostream>
#include <vector>

using namespace std;

const int mod = 1000000007;

int main() {
    freopen("ferate.in", "r", stdin);
	freopen("ferate.out", "w", stdout);

	int N, M, S;
	cin >> N >> M >> S;
	for(int i = 0 ; i < M ; i++) {
		int x, y;
		cin >> x >> y;
	}

	cout << 0;

    return 0;
}
